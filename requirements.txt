# Core dependencies
matplotlib>=3.5.0

# PDF generation from matplotlib
# The pattern will be saved as PDF using matplotlib's savefig

# OpenPattern - Required for pattern generation
# Installation: pip install git+https://github.com/fmetivier/OpenPattern.git
